import { prisma } from '@/lib/prisma';
import { redirect } from 'next/navigation';

export default function NewPostPage(){async function create(formData:FormData){'use server'; await prisma.blogPost.create({data:{title:String(formData.get('title')),slug:String(formData.get('slug')),excerpt:String(formData.get('excerpt')),coverImage:'',body:String(formData.get('body')),categories:String(formData.get('categories')||'').split(',').map(s=>s.trim()).filter(Boolean),tags:String(formData.get('tags')||'').split(',').map(s=>s.trim()).filter(Boolean),seoTitle:String(formData.get('seoTitle')||formData.get('title')),seoDescription:String(formData.get('seoDescription')||formData.get('excerpt')),state: formData.get('state')==='PUBLISHED' ? 'PUBLISHED':'DRAFT',publishDate:new Date()}}); redirect('/admin/posts');}
return <form action={create} className="grid gap-2 max-w-xl"><input name="title" placeholder="title" className="border p-2 rounded" required/><input name="slug" placeholder="slug" className="border p-2 rounded" required/><textarea name="excerpt" className="border p-2 rounded" required/><textarea name="body" className="border p-2 rounded" required/><input name="categories" className="border p-2 rounded" placeholder="category1, category2"/><input name="tags" className="border p-2 rounded" placeholder="tag1, tag2"/><input name="seoTitle" className="border p-2 rounded"/><input name="seoDescription" className="border p-2 rounded"/><select name="state" className="border p-2 rounded"><option value="DRAFT">Draft</option><option value="PUBLISHED">Published</option></select><button className="bg-ink text-paper rounded p-2">Create</button></form>;}
